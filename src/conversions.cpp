#include "Custom Extras/conversions.hpp" //No need for main because I only want that.

int convInt(float input) { //Converts input to an int
    return static_cast<int>(input);
}

int convInt(double input) { //Converts input to an int
    return static_cast<int>(input);
}

int convInt(char input) { //Converts input to an int
    return static_cast<int>(input);
}

float convFloat(int input) { //Converts input to a float
    return static_cast<float>(input);
}

float convFloat(double input) { //Converts input to a float
    return static_cast<float>(input);
}

float convFloat(char input) { //Converts input to a float
    return static_cast<float>(input);
}

double convDouble(int input) { //Converts input to a double
    return static_cast<double>(input);
}

double convDouble(float input) { //Converts input to a double
    return static_cast<double>(input);
}

double convDouble(char input) { //Converts input to a double
    return static_cast<double>(input);
}

char convChar(int input) { //Converts input to a char
    return static_cast<char>(input);
}

char convChar(float input) { //Converts input to a char
    return static_cast<char>(input);
}

char convChar(double input) { //Converts input to a char
    return static_cast<char>(input);
}
